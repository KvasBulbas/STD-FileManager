#ifndef FILEMANAGER_H
#define FILEMANAGER_H
#include <QFileInfo>
#include <vector>

class FileManager
{
public:
    FileManager();

    void checkFile()
    {

    }

private:
    std::vector<QFileInfo> abc;
};

#endif // FILEMANAGER_H
